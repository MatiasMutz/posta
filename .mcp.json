{
  "mcpServers": {
    "supabase": {
      "type": "http",
      "url": "https://mcp.supabase.com"
    },
    "context7": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"]
    }
  },
  "_comment_templates": {
    "playwright": {
      "description": "Navegador E2E — activar solo para desarrollo E2E real (specs Playwright); no en backend/migraciones; consume tokens",
      "example": {
        "cursor-ide-browser": {
          "type": "builtin",
          "note": "Ya provisto por Cursor cuando está habilitado"
        }
      }
    },
    "github": {
      "description": "PRs, issues, CI — requiere token",
      "example": {
        "github": {
          "type": "stdio",
          "command": "npx",
          "args": ["-y", "@modelcontextprotocol/server-github"],
          "env": {
            "GITHUB_PERSONAL_ACCESS_TOKEN": "<token>"
          }
        }
      }
    }
  }
}
